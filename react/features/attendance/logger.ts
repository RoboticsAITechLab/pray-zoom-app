/**
 * Custom feature scaffold – no logic yet.
 *
 * Logger for the attendance tracking feature.
 */

import { getLogger } from '../base/logging/functions';

export default getLogger('features/attendance');
