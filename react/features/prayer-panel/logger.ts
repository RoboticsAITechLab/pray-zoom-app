/**
 * Custom feature scaffold – no logic yet.
 *
 * Logger for the prayer panel feature.
 */

import { getLogger } from '../base/logging/functions';

export default getLogger('features/prayer-panel');
